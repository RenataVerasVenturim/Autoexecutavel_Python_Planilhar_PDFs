const uploadForm=document.getElementById("upload-form");function handleFiles(e){for(let l of e)console.log("File:",l.name)}uploadForm.addEventListener("dragover",e=>{e.preventDefault(),uploadForm.classList.add("highlight")}),uploadForm.addEventListener("dragleave",()=>{uploadForm.classList.remove("highlight")}),uploadForm.addEventListener("drop",e=>{e.preventDefault(),uploadForm.classList.remove("highlight");let l=e.dataTransfer.files;handleFiles(l)}),uploadForm.addEventListener("change",e=>{let l=e.target.files;handleFiles(l)}),uploadForm.addEventListener("drop",e=>{e.preventDefault(),uploadForm.classList.remove("highlight");let l=e.dataTransfer.files;handleFiles(l);let a=document.getElementById("pdf_file");a.files=l});
